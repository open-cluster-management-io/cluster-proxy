apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cluster-proxy-addon-agent-impersonator
rules:
{{- if eq .Values.enableImpersonation "true" }}
- apiGroups: [""]
  resources: ["users", "groups"]
  verbs: ["impersonate"]
{{- end }}
- apiGroups: ["authentication.k8s.io"]
  resources: ["tokenreviews"]
  verbs: ["create"]
