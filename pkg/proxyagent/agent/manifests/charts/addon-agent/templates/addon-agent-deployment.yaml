{{- $clusterProxyImage := include "cluster-proxy-common.clusterProxyImage" . }}
{{- $proxyAgentImage := include "cluster-proxy-common.proxyAgentImageOrDefault" . }}
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ .Values.agentDeploymentName }}
  {{- with .Values.agentDeploymentAnnotations }}
  annotations:
{{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      open-cluster-management.io/addon: cluster-proxy
      proxy.open-cluster-management.io/component-name: proxy-agent
  template:
    metadata:
      annotations:
      {{- with .Values.agentDeploymentAnnotations }}
{{- toYaml . | nindent 8 }}
      {{- end }}
        target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
      labels:
        open-cluster-management.io/addon: cluster-proxy
        proxy.open-cluster-management.io/component-name: proxy-agent
    spec:
      {{- if .Values.serviceProxyHost }}
      hostAliases:
      - ip: "127.0.0.1"
        hostnames:
        - {{ .Values.serviceProxyHost }}
      {{- end }}
      serviceAccount: cluster-proxy
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      {{- if .Values.tolerations }}
      tolerations: {{ toYaml .Values.tolerations | nindent 8 }}
      {{- end }}
      {{- if .Values.nodeSelector }}
      nodeSelector: {{ toYaml .Values.nodeSelector | nindent 8 }}
      {{- end }}
      containers:
        - name: proxy-agent
          {{- $reverseResourceRequirements := reverse .Values.global.resourceRequirements }}
          {{- range $item := $reverseResourceRequirements }}
            {{- if regexMatch $item.containerIDRegex "deployments:cluster-proxy-proxy-agent:proxy-agent" }}
          resources:
              {{- toYaml $item.resources | nindent 12 }}
              {{- break -}}
            {{- end -}}
          {{- end }}
          image: {{ $proxyAgentImage }}
          imagePullPolicy: IfNotPresent
          command:
            - /proxy-agent
          args:
            - --proxy-server-host={{ .Values.serviceEntryPoint }}
            - --proxy-server-port={{ .Values.serviceEntryPointPort }}
            - --agent-identifiers={{ .Values.agentIdentifiers }}
            - --ca-cert=/etc/ca/ca.crt
            - --agent-cert=/etc/tls/tls.crt
            - --agent-key=/etc/tls/tls.key
            {{- range .Values.additionalProxyAgentArgs }}
            - {{ . }}
            {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            runAsNonRoot: true
            readOnlyRootFilesystem: true
          livenessProbe:
            httpGet:
              path: /healthz
              scheme: HTTP
              port: 8888
            initialDelaySeconds: 10
            failureThreshold: 1
            periodSeconds: 10
          env:
            {{- if .Values.proxyConfig.HTTP_PROXY }}
            - name: HTTP_PROXY
              value: {{ .Values.proxyConfig.HTTP_PROXY }}
            {{- end }}
            {{- if .Values.proxyConfig.HTTPS_PROXY }}
            - name: HTTPS_PROXY
              value: {{ .Values.proxyConfig.HTTPS_PROXY }}
            {{- end }}
            {{- if .Values.proxyConfig.NO_PROXY }}
            - name: NO_PROXY
              value: {{ .Values.proxyConfig.NO_PROXY }}
            {{- end }}
            - name: ROOT_CA_CERT
              value: "/etc/ca/ca.crt"
          volumeMounts:
            - name: ca
              mountPath: /etc/ca
              readOnly: true
            - name: hub
              mountPath: /etc/tls
              readOnly: true
        - name: addon-agent
          {{- $reverseResourceRequirements := reverse .Values.global.resourceRequirements }}
          {{- range $item := $reverseResourceRequirements }}
            {{- if regexMatch $item.containerIDRegex "deployments:cluster-proxy-proxy-agent:addon-agent" }}
          resources:
              {{- toYaml $item.resources | nindent 12 }}
              {{- break -}}
            {{- end -}}
          {{- end }}
          image: {{ $clusterProxyImage }}
          imagePullPolicy: IfNotPresent
          command:
            - /agent
          args:
            - --v=2
            {{- range .Values.addonAgentArgs }}
            - {{ . }}
            {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            privileged: false
            runAsNonRoot: true
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: hub-kubeconfig
              mountPath: /etc/kubeconfig/
              readOnly: true
            - name: hub
              mountPath: /etc/tls
              readOnly: true
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
        {{- if .Values.enableServiceProxy }}
        {{- if ne (empty .Values.oidcIssuerURL) (empty .Values.oidcClientID) }}
          {{- fail "oidcIssuerURL and oidcClientID must be specified together" }}
        {{- end }}
        {{- if and .Values.oidcIssuerURL (ne .Values.enableImpersonation "true") }}
          {{- fail "oidcIssuerURL requires enableImpersonation=true" }}
        {{- end }}
        - name: service-proxy
          {{- $reverseResourceRequirements := reverse .Values.global.resourceRequirements }}
          {{- range $item := $reverseResourceRequirements }}
            {{- if regexMatch $item.containerIDRegex "deployments:cluster-proxy-proxy-agent:service-proxy" }}
          resources:
              {{- toYaml $item.resources | nindent 12 }}
              {{- break -}}
            {{- end -}}
          {{- end }}
          image: {{ $clusterProxyImage }}
          imagePullPolicy: IfNotPresent
          command:
            - /cluster-proxy
          args:
            - service-proxy
          {{- if .Values.additionalServiceCAConfigMap }}
            - --additional-service-ca=/additional-service-ca/service-ca.crt
          {{- end }}
            - --enable-impersonation={{ .Values.enableImpersonation }}
            - --cert=/server-cert/tls.crt
            - --key=/server-cert/tls.key
            - --hub-kubeconfig=/etc/kubeconfig/kubeconfig
          {{- if .Values.oidcIssuerURL }}
            {{- /* values such as prefixes may end with a colon, so quote every arg */}}
            - {{ printf "--oidc-issuer-url=%s" .Values.oidcIssuerURL | quote }}
            - {{ printf "--oidc-client-id=%s" .Values.oidcClientID | quote }}
            {{- if .Values.oidcUsernameClaim }}
            - {{ printf "--oidc-username-claim=%s" .Values.oidcUsernameClaim | quote }}
            {{- end }}
            {{- if .Values.oidcUsernamePrefix }}
            - {{ printf "--oidc-username-prefix=%s" .Values.oidcUsernamePrefix | quote }}
            {{- end }}
            {{- if .Values.oidcGroupsClaim }}
            - {{ printf "--oidc-groups-claim=%s" .Values.oidcGroupsClaim | quote }}
            {{- end }}
            {{- if .Values.oidcGroupsPrefix }}
            - {{ printf "--oidc-groups-prefix=%s" .Values.oidcGroupsPrefix | quote }}
            {{- end }}
            - {{ printf "--oidc-reserved-name-prefixes=%s" (join "," .Values.oidcReservedNamePrefixes) | quote }}
            {{- if .Values.oidcSigningAlgs }}
            - {{ printf "--oidc-signing-algs=%s" (join "," .Values.oidcSigningAlgs) | quote }}
            {{- end }}
            {{- if .Values.oidcRequiredClaimsJSON }}
              {{- $requiredClaims := mustFromJson .Values.oidcRequiredClaimsJSON }}
              {{- if not (kindIs "map" $requiredClaims) }}
                {{- fail "oidcRequiredClaimsJSON must be a JSON object" }}
              {{- end }}
              {{- range $claim, $value := $requiredClaims }}
                {{- if not (kindIs "string" $value) }}
                  {{- fail "oidcRequiredClaimsJSON values must be strings" }}
                {{- end }}
            - {{ printf "--oidc-required-claim=%s=%s" $claim $value | quote }}
              {{- end }}
            {{- end }}
            {{- if .Values.oidcCAConfigMap }}
            - --oidc-ca-file=/oidc-ca/ca.crt
            {{- end }}
          {{- end }}
            {{- range .Values.additionalServiceProxyArgs }}
            - {{ . }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 2
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 7443
            periodSeconds: 2
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            privileged: false
            runAsNonRoot: true
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: hub-kubeconfig
              mountPath: /etc/kubeconfig/
              readOnly: true
            - name: hub
              mountPath: /etc/tls
              readOnly: true
            {{- if .Values.additionalServiceCAConfigMap }}
            - name: additional-service-ca
              mountPath: /additional-service-ca
              readOnly: true
            {{- end }}
            {{- if and .Values.oidcIssuerURL .Values.oidcCAConfigMap }}
            - name: oidc-ca
              mountPath: /oidc-ca
              readOnly: true
            {{- end }}
            - name: service-proxy-server-cert
              mountPath: /server-cert
              readOnly: true
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
        {{- end }}
      volumes:
        - name: ca
          secret:
            secretName: cluster-proxy-ca
        - name: hub
          secret:
            secretName: cluster-proxy-open-cluster-management.io-proxy-agent-signer-client-cert
        - name: hub-kubeconfig
          secret:
            secretName: cluster-proxy-hub-kubeconfig
        {{- if .Values.enableServiceProxy }}
        {{- if .Values.additionalServiceCAConfigMap }}
        - name: additional-service-ca
          configMap:
            name: {{ .Values.additionalServiceCAConfigMap }}
            optional: true
        {{- end }}
        {{- if and .Values.oidcIssuerURL .Values.oidcCAConfigMap }}
        - name: oidc-ca
          configMap:
            name: {{ .Values.oidcCAConfigMap }}
            optional: true
        {{- end }}
        - name: service-proxy-server-cert
          secret:
            secretName: cluster-proxy-service-proxy-server-certificates
        {{- end }}
      {{- with .Values.proxyAgentImagePullSecrets }}
      imagePullSecrets:
      {{- range . }}
      - name: {{ . }}
      {{- end }}
      {{- end }}
